void	*ft_memset(void *b, int c, size_t len)
{
	int i;

	i = 0;
	while (i < len)
	{
		b[i] = (unsigned char)c;
		i++;
	}
	return (b);
}

int main(void)
{
	char *b = "hello";
	int c = 5;
	size_t len = sizeof(unsigned char);
	printf("%s", ft_memset(b, c, len));
	return (0);
}
